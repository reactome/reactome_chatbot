#!/usr/bin/env python3
"""Entry point for the routing probe; see src/evaluation/routing_probe.py."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))

from evaluation.routing_probe import main

if __name__ == "__main__":
    main()
