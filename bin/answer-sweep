#!/usr/bin/env python3
"""Entry point for the answer sweep; see src/evaluation/answer_sweep.py."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))

from evaluation.answer_sweep import main

if __name__ == "__main__":
    main()
